import { Text } from "react-native";

import ThemedView from "@shared/ui/ThemedView";

const CreateTracker = () => {
  return (
    <ThemedView className="flex-1">
      <Text>Create Tracker</Text>
    </ThemedView>
  );
};

export default CreateTracker;
